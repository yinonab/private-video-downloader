name: private_video_downloader
description: Private video downloader — Android MVP client
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.7.0
  flutter_secure_storage: ^9.2.4
  shared_preferences: ^2.3.3
  receive_sharing_intent: ^1.8.1
  path_provider: ^2.1.5
  media_store_plus: ^0.1.3
  open_filex: ^4.5.2
  share_plus: ^10.1.2
  uuid: ^4.5.1
  intl: ^0.20.2
  path: ^1.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
